# Production: set to your Render (or other) API origin, no trailing slash.
# Example: https://your-api.onrender.com
# Leave unset for local dev — Vite proxies /api to http://127.0.0.1:8000
VITE_API_URL=
